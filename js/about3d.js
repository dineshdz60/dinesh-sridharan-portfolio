(function(){if(typeof THREE==='undefined')return;
const pc=document.getElementById('pageCanvas');
if(pc){const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(50,window.innerWidth/(window.innerHeight*.8),.1,100);camera.position.z=6;const renderer=new THREE.WebGLRenderer({canvas:pc,antialias:true,alpha:true});renderer.setSize(window.innerWidth,window.innerHeight*.8);renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));renderer.setClearColor(0x000000,0);const total=40;const helixMeshes=[];for(let i=0;i<total;i++){const t=(i/total)*Math.PI*4;const geo=new THREE.SphereGeometry(.08+Math.random()*.06,8,8);const mat=new THREE.MeshPhongMaterial({color:0x2233aa,specular:0xffffff,shininess:200,transparent:true,opacity:.5+Math.random()*.4});const m=new THREE.Mesh(geo,mat);m.position.set(Math.cos(t)*3,(i/total)*8-4,Math.sin(t)*1.5);m.userData.t=t;m.userData.i=i;scene.add(m);helixMeshes.push(m);}const gemGeo=new THREE.IcosahedronGeometry(1.8,1);const gemMat=new THREE.MeshPhongMaterial({color:0x111122,specular:0xffffff,shininess:250,flatShading:true,transparent:true,opacity:.5});const gem=new THREE.Mesh(gemGeo,gemMat);gem.position.set(3,0,0);scene.add(gem);scene.add(new THREE.AmbientLight(0xffffff,.2));const pl1=new THREE.PointLight(0xffffff,3,25);pl1.position.set(5,5,5);scene.add(pl1);const pl2=new THREE.PointLight(0x2244ff,2,20);pl2.position.set(-5,-3,3);scene.add(pl2);let t=0;(function animate(){requestAnimationFrame(animate);t+=.006;helixMeshes.forEach(m=>{const angle=m.userData.t+t;m.position.x=Math.cos(angle)*3;m.position.z=Math.sin(angle)*1.5;});gem.rotation.y=t*.4;gem.rotation.x=Math.sin(t*.3)*.2;pl1.position.x=Math.sin(t*.5)*6;pl1.position.y=Math.cos(t*.4)*4;renderer.render(scene,camera);})();window.addEventListener('resize',()=>{camera.aspect=window.innerWidth/(window.innerHeight*.8);camera.updateProjectionMatrix();renderer.setSize(window.innerWidth,window.innerHeight*.8);});}
const portraitCanvas=document.getElementById('portraitCanvas');
if(portraitCanvas){const w=portraitCanvas.parentElement.offsetWidth||320;const h=portraitCanvas.parentElement.offsetHeight||400;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(50,w/h,.1,100);camera.position.z=4;const renderer=new THREE.WebGLRenderer({canvas:portraitCanvas,antialias:true,alpha:true});renderer.setSize(w,h);renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));renderer.setClearColor(0x000000,0);const geo=new THREE.IcosahedronGeometry(1.5,1);const mat=new THREE.MeshPhongMaterial({color:0x1a1a3a,specular:0xffffff,shininess:200,flatShading:true,transparent:true,opacity:.6});const mesh=new THREE.Mesh(geo,mat);scene.add(mesh);scene.add(new THREE.Mesh(geo.clone(),new THREE.MeshBasicMaterial({color:0x4466ff,wireframe:true,transparent:true,opacity:.15})));scene.add(new THREE.AmbientLight(0xffffff,.2));const pl=new THREE.PointLight(0xffffff,3,15);pl.position.set(3,3,3);scene.add(pl);scene.add(new THREE.PointLight(0x4466ff,2,10));let t=0;(function animate(){requestAnimationFrame(animate);t+=.01;mesh.rotation.y=t*.3;mesh.rotation.x=Math.sin(t*.4)*.25;renderer.render(scene,camera);})();}
const cc=document.getElementById('ctaCanvas');if(cc){const parent=cc.parentElement;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(60,parent.offsetWidth/(parent.offsetHeight||400),.1,100);camera.position.z=5;const renderer=new THREE.WebGLRenderer({canvas:cc,antialias:true,alpha:true});renderer.setSize(parent.offsetWidth,parent.offsetHeight||400);renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));renderer.setClearColor(0x000000,0);const meshes=[];for(let i=0;i<12;i++){const geo=new THREE.OctahedronGeometry(.2+Math.random()*.5,0);const mat=new THREE.MeshPhongMaterial({color:0x111133,specular:0xffffff,shininess:150,flatShading:true,transparent:true,opacity:.4+Math.random()*.4});const m=new THREE.Mesh(geo,mat);m.position.set((Math.random()-.5)*12,(Math.random()-.5)*6,(Math.random()-.5)*4);m.userData.s=.006+Math.random()*.01;m.userData.o=Math.random()*Math.PI*2;scene.add(m);meshes.push(m);}scene.add(new THREE.AmbientLight(0xffffff,.3));scene.add(new THREE.PointLight(0xffffff,2,30));let t2=0;(function animate(){requestAnimationFrame(animate);t2+=.008;meshes.forEach(m=>{m.rotation.x+=m.userData.s;m.rotation.y+=m.userData.s*.7;m.position.y+=Math.sin(t2+m.userData.o)*.004;});renderer.render(scene,camera);})();}
})();
