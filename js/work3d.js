(function(){if(typeof THREE==='undefined')return;
const pc=document.getElementById('pageCanvas');
if(pc){const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(50,window.innerWidth/(window.innerHeight*.7),.1,100);camera.position.z=6;const renderer=new THREE.WebGLRenderer({canvas:pc,antialias:true,alpha:true});renderer.setSize(window.innerWidth,window.innerHeight*.7);renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));renderer.setClearColor(0x000000,0);const shapes=[];const geos=[new THREE.IcosahedronGeometry(.4,0),new THREE.OctahedronGeometry(.5,0),new THREE.TetrahedronGeometry(.5,0),new THREE.BoxGeometry(.6,.6,.6)];for(let i=0;i<20;i++){const geo=geos[i%geos.length];const mat=new THREE.MeshPhongMaterial({color:0x111133,specular:0x8888ff,shininess:120,flatShading:true,transparent:true,opacity:.3+Math.random()*.5});const m=new THREE.Mesh(geo,mat);m.position.set((Math.random()-.5)*16,(Math.random()-.5)*10,(Math.random()-.5)*8);m.userData.speed={(x:(Math.random()-.5)*.01,y:(Math.random()-.5)*.01)};m.userData.float=Math.random()*Math.PI*2;scene.add(m);shapes.push(m);}scene.add(new THREE.AmbientLight(0xffffff,.2));const pl=new THREE.PointLight(0xffffff,3,30);pl.position.set(0,5,8);scene.add(pl);const pl2=new THREE.PointLight(0x2244ff,1.5,20);pl2.position.set(-8,-3,4);scene.add(pl2);let t=0;(function animate(){requestAnimationFrame(animate);t+=.008;shapes.forEach(m=>{m.rotation.x+=m.userData.speed.x;m.rotation.y+=m.userData.speed.y;m.position.y+=Math.sin(t+m.userData.float)*.003;});renderer.render(scene,camera);})();window.addEventListener('resize',()=>{camera.aspect=window.innerWidth/(window.innerHeight*.7);camera.updateProjectionMatrix();renderer.setSize(window.innerWidth,window.innerHeight*.7);});}
document.querySelectorAll('.mini-canvas').forEach(canvas=>{const shape=canvas.dataset.shape||'sphere';const parent=canvas.parentElement;const w=parent.offsetWidth||500;const h=parent.offsetHeight||400;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(45,w/h,.1,100);camera.position.z=4;const renderer=new THREE.WebGLRenderer({canvas,antialias:true,alpha:true});renderer.setSize(w,h);renderer.setPixelRatio(Math.min(window.devicePixelRatio,1.5));renderer.setClearColor(0x000000,0);let geo;if(shape==='pyramid')geo=new THREE.ConeGeometry(1.2,2.2,4,1);else if(shape==='sphere')geo=new THREE.IcosahedronGeometry(1.4,1);else if(shape==='box')geo=new THREE.BoxGeometry(1.8,1.8,1.8);else if(shape==='hexagon')geo=new THREE.CylinderGeometry(1.4,1.4,.4,6,1);else if(shape==='torus')geo=new THREE.TorusGeometry(1.2,.4,8,32);else if(shape==='diamond')geo=new THREE.OctahedronGeometry(1.5,0);else geo=new THREE.IcosahedronGeometry(1.4,0);const mat=new THREE.MeshPhongMaterial({color:0x1a1a2e,specular:0xffffff,shininess:200,flatShading:true,transparent:true,opacity:.65});const mesh=new THREE.Mesh(geo,mat);scene.add(mesh);scene.add(new THREE.Mesh(geo.clone(),new THREE.MeshBasicMaterial({color:0xffffff,wireframe:true,transparent:true,opacity:.07})));scene.add(new THREE.AmbientLight(0xffffff,.15));const pl1=new THREE.PointLight(0xffffff,3,20);pl1.position.set(4,4,4);scene.add(pl1);scene.add(new THREE.PointLight(0x4466ff,1.5,15));let t=Math.random()*Math.PI*2;(function animate(){requestAnimationFrame(animate);t+=.008;mesh.rotation.y=t*.4;mesh.rotation.x=Math.sin(t*.25)*.35;renderer.render(scene,camera);})();});
const cc=document.getElementById('ctaCanvas');if(cc){const parent=cc.parentElement;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(60,parent.offsetWidth/(parent.offsetHeight||400),.1,100);camera.position.z=5;const renderer=new THREE.WebGLRenderer({canvas:cc,antialias:true,alpha:true});renderer.setSize(parent.offsetWidth,parent.offsetHeight||400);renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));renderer.setClearColor(0x000000,0);const meshes=[];for(let i=0;i<10;i++){const geo=new THREE.IcosahedronGeometry(.2+Math.random()*.5,0);const mat=new THREE.MeshPhongMaterial({color:0x1a1a2e,specular:0xffffff,shininess:120,flatShading:true,transparent:true,opacity:.4+Math.random()*.4});const m=new THREE.Mesh(geo,mat);m.position.set((Math.random()-.5)*12,(Math.random()-.5)*6,(Math.random()-.5)*4);m.userData.s=.005+Math.random()*.012;m.userData.o=Math.random()*Math.PI*2;scene.add(m);meshes.push(m);}scene.add(new THREE.AmbientLight(0xffffff,.3));scene.add(new THREE.PointLight(0xffffff,2,30));let t=0;(function animate(){requestAnimationFrame(animate);t+=.008;meshes.forEach(m=>{m.rotation.x+=m.userData.s;m.rotation.y+=m.userData.s*.6;m.position.y+=Math.sin(t+m.userData.o)*.004;});renderer.render(scene,camera);})();}
})();
